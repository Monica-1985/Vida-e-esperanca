# Projeto ONG Vida e Esperança

![Status do Projeto](https://github.com/Monica-1985/vida-e-esperanca.git)

Este é o repositório oficial do site institucional da **ONG Vida e Esperança**, desenvolvido como projeto final da [Nome da Matéria/Curso]. O projeto consolida práticas profissionais de desenvolvimento front-end, incluindo versionamento com GitFlow, acessibilidade Nível AA (WCAG 2.1) e otimização para produção.

---

## 🚀 Acesso ao Projeto

O site está publicado e pode ser acessado através do GitHub Pages:

**[https://monica-1985.github.io/mvidaeesperanca/](https://monica-1985.github.io/mvidaeesperanca/)**

---

## ✨ Funcionalidades Principais

* **Design Responsivo:** O site se adapta a diferentes tamanhos de tela (desktop, tablet e mobile).
* **Acessibilidade (WCAG 2.1):** O projeto foi construído com foco em semântica e acessibilidade, incluindo:
    * Navegação completa por teclado.
    * Alto contraste de cores (Nível AA).
    * Suporte a leitores de tela com `aria-labels` e `aria-expanded`.
* **Modo Escuro (Dark Mode):** Um alternador de tema acessível que salva a preferência do usuário no `localStorage`.
* **Formulário de Cadastro:** Página de cadastro com máscaras de formulário (CPF, CEP, Telefone) implementadas de forma acessível (evento `blur`).

---

## 🛠️ Tecnologias Utilizadas

* **HTML5 Semântico:** Estruturação do site com tags (`<header>`, `<main>`, `<nav>`, etc.).
* **Tailwind CSS:** Framework utility-first para estilização rápida e responsiva.
* **JavaScript (ES6+):** Para interatividade, como o menu mobile, dark mode e máscaras de formulário.
* **Git & GitHub:** Para controle de versão, histórico e gerenciamento do projeto.
* **Node.js/npm:** Utilizado para o processo de build (minificação).
* **Terser:** Minificador de JavaScript.

---

## 📦 Como Rodar o Projeto Localmente

Para rodar o projeto e executar o processo de build:

1.  **Clone o repositório:**
    ```bash
    git clone [URL-DO-SEU-REPO-NO-GITHUB]
    cd [NOME-DO-SEU-REPOSITORIO]
    ```

2.  **Instale as dependências de desenvolvimento:**
    ```bash
    npm install
    ```

3.  **Execute o script de build:**
    (Este comando irá gerar os arquivos minificados em `/dist`)
    ```bash
    npm run build
    ```

4.  **Abra o site:**
    Basta abrir o arquivo `index.html` no seu navegador.

---

## 🎯 Conformidade com os Requisitos da Entrega

Este projeto foi desenvolvido para atender às especificações técnicas obrigatórias:

### 1. Controle de Versão (Git/GitHub)

* **Estratégia de Branching:** O repositório segue a metodologia **GitFlow**, com branches `main` (produção), `develop` (desenvolvimento) e `feature/` (novas implementações).
* **Histórico de Commits:** Todos os commits seguem o padrão de **Commits Semânticos** (ex: `feat:`, `fix:`, `style:`, `refactor:`, `docs:`) para um histórico claro e organizado.
* **Issues e Milestones:** O desenvolvimento foi rastreado através das `Issues` do GitHub.
* **Pull Requests (PRs):** Todo código novo foi integrado ao `develop` através de PRs documentados.
* **Releases:** O projeto utiliza o sistema de `Releases` do GitHub para marcar versões estáveis (ex: `v1.0.0`).

### 2. Acessibilidade (WCAG 2.1 Nível AA)

* **Estrutura Semântica:** Uso correto de tags HTML5.
* **Navegação por Teclado:** O site é 100% navegável utilizando a tecla `Tab`.
* **Contraste de Cores:** Todos os textos atingem o nível de contraste mínimo de **4.5:1** (ou 3:1 para textos grandes), corrigindo falhas anteriores (ex: `text-white` em `bg-blue-800`).
* **Suporte a Leitores de Tela:**
    * Botões de ícone (menu, tema) possuem `aria-label` e `aria-expanded` (quando aplicável).
    * As máscaras de formulário foram movidas para o evento `blur` para não interromper a digitação.
* **Modo Escuro Acessível:** A versão de alto contraste (modo escuro) também foi validada e cumpre os requisitos de contraste.

### 3. Otimização para Produção

* **Minificação de CSS/JS:** O script CDN do Tailwind foi removido. O projeto utiliza um processo de `build` (`npm run build`) que usa **Tailwind CLI** e **Terser** para gerar arquivos `dist/style.min.css` e `dist/main.min.js` minificados.
* **Compressão de Imagens:** As imagens do projeto foram comprimidas (utilizando ferramentas como TinyPNG) para otimizar o carregamento.

---

## ✒️ Autor

Mônica [SEU SOBRENOME AQUI]